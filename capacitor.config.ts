import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'projetoIntegrador',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
